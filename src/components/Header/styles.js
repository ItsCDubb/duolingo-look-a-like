import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  root: {
    flexDirection: "row",
    alignItems: "center",
  },
  icon: {
    marginHorizontal: 10,
    height: 30,
    width: 30,
  },
  lives: {
    color: "red",
    fontSize: 18,
    fontWeight: "bold",
  },
});

export default styles;
